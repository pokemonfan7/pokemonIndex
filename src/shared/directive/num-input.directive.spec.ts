import { NumInputDirective } from './num-input.directive';

describe('NumInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NumInputDirective();
    expect(directive).toBeTruthy();
  });
});
